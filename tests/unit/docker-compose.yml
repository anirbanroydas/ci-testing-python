version: "3"

services:

  identidock_unit_tester:
    build: ../..
    image: aroyd/identidock_unit_test:${IDENTIDOCK_UNIT_TEST_IMAGE_TAG:-latest}
    environment:
      ENV: UNIT_TEST 
    # volumes:
    #   - .:/project


