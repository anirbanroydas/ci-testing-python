function (request, state) {
    var mocked_png_img = Buffer.from('I am a mocked image', 'binary')
    
    return {
    	headers: {
        	'Content-Type': 'image/png'
      	},
      	body: mocked_png_img
    };
}